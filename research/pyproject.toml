[tool.poetry]
name = "research"
version = "0.1.0"
description = ""
authors = ["Sandi <sandi.dusic@protonmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
